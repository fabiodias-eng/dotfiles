#!/bin/bash

path="$1"
fg="$2"
bg="$3"

branch=$(git -C "$path" branch --show-current 2>/dev/null)

if [ -n "$branch" ]; then
    printf '#[fg=%s,bg=%s,bold]  %s #[default]' "$fg" "$bg" "$branch"
fi
